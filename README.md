# Krishak-G

A voice-first field assistant for farmers, powered by Rime TTS

## Setup

### Backend

Navigate to the `backend` directory and run:

```bash
cd backend
npm install
npm run dev
```

### Frontend

Navigate to the `frontend` directory and run:

```bash
cd frontend
npm install
npm run dev
```
